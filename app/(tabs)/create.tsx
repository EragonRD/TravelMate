import { View } from 'react-native';
import React from 'react';

// This is a dummy component to satisfy the Tab Router for the "create" route.
// The actual navigation is handled by the tabPress listener in _layout.tsx
export default function CreatePlaceholder() {
    return <View />;
}
